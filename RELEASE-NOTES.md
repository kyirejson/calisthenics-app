# Uncover v1.3.0

App version: 1.3.0
Android versionCode: 4

独立 Android 安装包，无需 Expo Go。启用专属签名和 `production` 在线更新渠道，后续版本持续使用同一签名。

## 本次更新

- 整合今日训练与日程，支持动作指导、训练加组、追加动作和当天重置。
- 历史只保存已勾选的有效组数；未训练退出不产生力量训练记录。
- 优化训练课程、动作进阶与营养展示，加入单人动作示范图。
- 支持检查新 APK、下载兼容内容更新及用户确认重启；训练中不强制重启。

## 安装

用安卓手机下载 APK，打开后按系统提示允许安装。本项目当前按全新用户发布，不提供旧测试版兼容或迁移流程。

## 资源与验证边界

- 本包不包含授权尚未确认的原书全文、原图与照片，部分动作会显示缺图提示。
- 已执行类型检查、训练/更新逻辑测试和 Android 云端构建；实体安卓设备上的安装、返回键、GPS、记录持久化及热更新端到端流程尚待验收。
- 依赖审计仍报告 10 项中等级别问题（0 高危、0 严重）；未执行涉及跨 SDK 版本变化的强制自动修复，不声称依赖零风险。
- 尚未发布内容热更新，安装后检查更新可能显示当前为最新版。
- 个人训练数据默认仅在本机，支持 JSON 导出，尚无导入恢复；不要将本 App 当作医疗诊断工具。

旧版本保留归档，不再推荐下载。本次为 GitHub APK 正式发布，不代表已上架手机应用商店。
